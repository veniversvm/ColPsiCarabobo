# AI Context: pkg/database/

## Purpose: Database lifecycle management (connect, seed, migrate)

## Key Functions
- `InitDatabase() *gorm.DB` — Opens and configures the GORM connection
- `SeedAdminUsers(db)` — Creates default admin user if not present
- `InitMigrations(db)` — AutoMigrate all models + UNIQUE constraints

## Connection: GORM + PgBouncer (transaction mode), SSL configurable

## Pool Configuration
- MaxOpenConns: 10
- MaxIdleConns: 5
- ConnMaxLifetime: 1 hour

## Seed: Default admin (admin@colpsi.com, ADMINISTRADOR role, ColPsi ColPsi)

## Migrate: AutoMigrate all models + UNIQUE constraints on cedula, email, phone

## Tables (17)
psi_users, psi_user_col_data, psi_user_audit_models, psi_user_social_networks,
psi_user_solvency, psi_user_psi_specialty, psi_user_psi_specialty_relations,
psi_user_deontologia, text_models, post_grades, user_admins, user_admin_audit_models,
page_views, profile_views, search_events, login_events, active_sessions

## Used By: cmd/api/main.go, internal/repository/postgres/*
