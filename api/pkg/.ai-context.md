# AI Context: pkg/

## Purpose: Shared reusable libraries (database, S3) with no internal dependencies

## Key Dependencies: GORM, AWS SDK, PgBouncer

## Architecture Rule: pkg/ NEVER imports from internal/

## Files
- `postgres.go` — GORM+PgBouncer connection, InitDatabase
- `seed.go` — Admin user seed
- `migration.go` — AutoMigrate+unique constraints
- `s3.go` — AWS S3/MinIO client init
- `upload.go` — UploadImage, UploadImageToS3 with multipart

## Known Patterns: Singleton config via config.InitConfig(), env-based configuration, transaction wrapping for data integrity

## Consumer: internal/service/*, internal/repository/postgres/*, internal/handler/*
