# AI Context: cmd/
## Purpose: Application entry points (Go project layout convention)
## Structure: cmd/api/ (main server), cmd/exp/migrate/ (migration tool)
## Entry Points: main.go (Fiber server on :8080), migrate/main.go (Atlas schema export)
## Initialization Chain: env.InitConfig() → database.InitDatabase() → database.SeedAdminUsers() → database.InitMigrations() → s3.InitS3() → router.InitRouter()
## Used By: docker-compose.yml (api service, migrador service)
