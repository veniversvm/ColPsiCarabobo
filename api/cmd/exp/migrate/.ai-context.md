# AI Context: cmd/exp/migrate/
## Purpose: Generate GORM-compatible schema for Atlas migration diffs
## File: main.go
## Function: generateSchema() - SQLite temp DB + AutoMigrate + schema dump
## Workflow: Developer runs this → outputs SQL schema → atlas diff compares with DB → generates migration
## Models Migrated: UserAdmin, PsiUserModel, PsiUserColData, TextModel, PsiSpecialtyModel, PsiUserSocialNetwork, PostGrade, PsiUserSolvency, PsiUserDeontologia, PsiUserAuditModel, UserAdminAuditModel, PageView, ProfileView, SearchEvent, LoginEvent, ActiveSession
