# AI Context: internal/utils/
## Purpose: Pure utility functions with no business logic dependencies
## Functions (10):
  - christ(): ASCII art console branding
  - CleanAlphaNumeric(str): Strip non-alphanumeric chars (rune-based, Unicode-safe)
  - NormalizeGeographicLocation(location): Venezuelan municipality/state name normalization
  - BoolFromForm(value): Convert form string to bool
  - SanitizeAndCompressImage(file): Image → WebP compression, transparency flatten, 5MB limit, magic bytes validation
  - IsRequestEmpty(struct): Reflection-based empty struct detection (prevents empty PATCH)
  - NormalizePlatformName(name): Social media name normalization ("ig"→"Instagram")
  - GenerateRandomString(length): crypto/rand secure random strings
  - ValidatePasswordStrength(password): Min 8 chars, uppercase, lowercase, digit, special char
  - ValidateEmail(email): Regex-based email validation
## Security Notes: Image validation uses magic bytes (not just extension), crypto/rand for randomness
## Testing: utils_test.go covers all functions
## Pattern: All functions are pure (no side effects, no DB access)
## Used By: service layer, handler layer, middleware
