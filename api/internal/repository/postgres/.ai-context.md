# AI Context: internal/repository/postgres/
## Purpose: GORM implementations of all domain repository interfaces
## Architecture Role: Concrete data access — implements contracts from domain/
## Files: psi_repository.go, post_repo.go, user_admin_repo.go, specialty_repo.go
## Pattern: Each repo receives *gorm.DB, implements domain interface methods
## Key Techniques:
  - ILIKE unaccent(?) for Spanish accent-insensitive search
  - GORM Preload for eager loading relationships
  - Transactions (db.Begin/Commit/Rollback) for multi-table operations
  - Soft deletes via gorm.DeletedAt
  - Privacy Shield: Select() to exclude sensitive columns for public queries
  - Unique constraints enforced at DB level (cedula, email, phone)
## Transaction Pattern: tx := db.Begin() → defer tx.Rollback() → operations → tx.Commit()
## Error Handling: Returns domain errors that map to user-friendly messages via ErrorMapper
## Dependencies: domain interfaces, GORM, database connection
## Used By: service layer (via interface), cmd/api/main.go (initialization)
